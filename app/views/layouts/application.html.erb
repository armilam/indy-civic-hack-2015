<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "myIPS Magnet/Choice Program Portal" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Rails Devise" %>">
    <%= stylesheet_link_tag 'application', media: 'all', data: { turbolinks_track: true } %>
    <link href='http://fonts.googleapis.com/css?family=Lato:400,700,400italic,700italic' rel='stylesheet' type='text/css'>
    <%= javascript_include_tag 'application', data: { turbolinks_track: true } %>
    <%= csrf_meta_tags %>
  </head>
  <body class="<%= content_for(:body_class) %>">
    <header>
      <%= render 'layouts/navigation' %>
    </header>
    <main role="main">
      <div class="container-fluid">
       <%= render 'layouts/messages' %>
       <%= yield %>
      </div>
    </main>
    <footer class="site-footer" style="padding: 1em;">
      <p>
        <%= link_to "Visit the IPS main page", "http://www.myips.org/" %>
        • <%= link_to "Terms of Service", "#" %>
        • <%= link_to "Contact Us", "#" %>
      </p>

      <p>Made with ♥ by Lesson.ly Llamas</p>
    </footer>
  </body>
</html>
